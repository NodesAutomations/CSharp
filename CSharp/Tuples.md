### Basics
- Tuples Type Provide way to group multiple Data elements into light weight structure
```csharp
(int Id, string Name) Book=(1,"The Way of King");
Console.WriteLine($"{Book.Id}-{Book.Name}");
```
